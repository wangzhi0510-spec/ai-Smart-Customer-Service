# AI 智能客服系统项目说明

## 项目定位

本项目是面向企业知识库的多轮 AI 客服系统，首版核心闭环为：注册登录、独立会话、知识文档上传、异步解析与向量化、FQA 优先、混合检索 RAG、证据型 Prompt、DashScope 流式回答、来源引用、历史消息和反馈。

## 技术选型

- 前端：Vue 3 + TypeScript + Vite + Pinia，保持前后端分离。
- 后端：Python 3.12 + FastAPI + Pydantic v2 + SQLAlchemy 2.x + Alembic。
- 数据库：MySQL 存储用户、会话、消息、文档元数据和反馈；Redis 用于配额与 Celery 队列。
- 向量库：Milvus，保存带 user_id、document_id、version、parent_id 的 Dense/Sparse 向量。
- 模型：本地 `bge-m3` 生成 Dense/Sparse Embedding，本地 `bge-reranker-large` 重排，DashScope `qwen-plus` 生成答案。模型只读挂载，不进入 Docker 镜像或 Git 历史。

## AI 架构

用户问题先经过鉴权、长度校验和每日配额预占，然后执行精确 FQA；未命中时同时进行 Dense 与 Sparse 检索，经 RRF 融合、子块去重、父块回收和重排，最后构造证据型 Prompt 调用 LLM。后端使用 POST + SSE 发送 `start`、多次 `delta`、`source`、`done` 或 `error` 事件，前端逐段显示回答。

## AI 工程问题处理

- 检索为空：返回“知识库暂无足够信息回答该问题”，不调用 LLM 编造答案。
- 幻觉控制：System Prompt 要求业务事实只能来自上下文，来源只能引用实际检索片段，规则冲突必须披露。
- 上下文超长：按 token 预算优先保留检索证据，再截断最旧历史；同一父块的子块聚合，减少注意力稀释。
- 版本一致性：文档更新创建新版本，仅 active version 参与检索；删除同步清理文件、数据库可见性和 Milvus 向量。
- 模型/基础设施异常：Adapter 捕获超时、限流和连接错误，返回稳定错误码，不泄露密钥、连接串或堆栈；重排不可用时保留 RRF 顺序并记录 warning。

## 质量验证

测试包括 pytest 后端单元/集成测试、Vitest 前端测试、Playwright 核心流程和离线 RAG 评测集。评估维度包括命中率、来源正确性、fallback 正确性、延迟和删除后不可检索。真实模型评测需要本地模型和 DashScope Key，自动化测试默认使用 Fake Provider。

## AI 工具使用说明

开发过程中使用 Codex 编程助手完成代码检索、测试设计、实现和审查；使用 DashScope API 作为运行时 LLM。所有 AI 生成代码都经过人工检查，重点修正了用户资源隔离、Milvus 删除一致性、SSE 真流式协议、上下文截断和错误码边界。核心 RAG 检索、RRF、父块回收和 Prompt 规则均保留在项目代码中，没有把逻辑隐藏在不可审计的黑盒 Chain 中。

## 首版与第二阶段边界

图片识别、追问建议、多知识库路由、HyDE、SubQuery、Backtracking、RAGAS 和代码修改 Agent 仅保留设计或真实接口边界，首版不伪装为已实现。

## 交付物索引

- 后端：`backend/`，含 FastAPI、Alembic、Worker、测试和 README。
- 前端：`frontend/`，含 Vue 页面、组件、Pinia、测试和 README。
- 文档：`docs/`，含 API、数据库、AI 架构、业务流程、部署、测试和 Agent 挑战说明。
- 示例知识库：`examples/knowledge_base/`，三份中文文档共约 3200 字。
- 模型说明：`model/README.md` 与 `model/MANIFEST.sha256`；权重保留在本地，不上传 GitHub。