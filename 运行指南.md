# AI 智能客服系统运行指南

## 1. 前置条件

安装 Docker Desktop、Python 3.12、Node.js/npm，并准备本地模型目录。当前开发机模型已复制到 `D:\code\codex_code\cs_rag_agent\model`，约 4.7GB。新机器可通过 `MODEL_HOST_PATH` 指向其他目录。

## 2. 配置环境

```powershell
cd D:\code\codex_code\cs_rag_agent
Copy-Item .env.example .env
```

在 `.env` 中填写 MySQL、Redis、Milvus、MinIO 凭据、随机 `JWT_SECRET_KEY` 和后端使用的 `DASHSCOPE_API_KEY`。真实密钥只保存在本地 `.env`，不要提交 GitHub。模型路径使用：

```text
MODEL_HOST_PATH=./model
```

## 3. 启动 Docker

```powershell
docker compose up -d mysql redis etcd minio milvus
docker compose --profile app up -d --build
```

如果宿主机已有其他 Compose 项目占用 3306、6379、9000、9091、19530 或 5173，请修改 `.env` 中端口，或继续使用已有的 `cs-rag-local` 实例。

## 4. 访问地址

- 前端：<http://localhost:5173>
- 后端存活检查：<http://localhost:8000/api/v1/health/live>
- Milvus：`localhost:19530`
- MinIO：<http://localhost:9001>

## 5. 初始化示例知识库

仓库包含 `examples/knowledge_base/公司产品介绍.txt`、`常见问题FAQ.md`、`退换货政策.txt`，合计约 3200 字。当前安全默认是手动导入：先注册一个账户，再从前端知识库页面上传三份文件，等待状态变为“就绪”后测试问答。这样不会在启动时使用固定弱密码创建共享账号。

自动 seed 设计已记录在 `docs/superpowers/specs/2026-08-31-demo-knowledge-bootstrap-design.md`，待确认演示账号密码和是否允许启动时写入数据后启用。启用时必须通过环境变量提供 `DEMO_USER_PASSWORD`，不得把密码写入仓库。

## 6. 本地开发模式

```powershell
.venv\Scripts\python.exe -m alembic -c backend\alembic.ini upgrade head
.venv\Scripts\python.exe -m uvicorn backend.app.main:app --reload --port 8000
npm run dev --prefix frontend -- --host 127.0.0.1 --port 5173
```

## 7. 验证命令

```powershell
.venv\Scripts\python.exe -m pytest -q
npm test --prefix frontend -- --run
npm run build --prefix frontend
docker compose config --quiet
docker compose --profile app config --quiet
git diff --check
```

## 8. 停止服务

```powershell
docker compose --profile app down
```

不加 `-v` 会保留数据库、对象存储和 Milvus 数据卷；不要在没有备份时执行 `down -v`。
确认账号后可手动执行：

```powershell
$env:DEMO_USER_IDENTIFIER='demo@example.com'
$env:DEMO_USER_PASSWORD='请替换为强密码'
.venv\Scripts\python.exe -m backend.scripts.seed_demo_knowledge --knowledge-dir examples\knowledge_base
```
